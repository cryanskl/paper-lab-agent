# pnpm 10 disables postinstall scripts by default; explicitly allow
# the packages we trust (better-sqlite3 needs node-gyp to build the
# native binding, and Next.js uses sharp for image optimization).
enable-pre-post-scripts=true
